Implementation of CYK parsing algorithm.

The program takes as input
 - the grammar in CNF
 - the string to be parsed

The algorithm is implemented with dynamic programming.

A demo execution is:

	$ ./CYK grammar.cnf
